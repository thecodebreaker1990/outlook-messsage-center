import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  typography: {
    fontSize: 14 // This sets the base font size to 14px
    // You can also customize other typography properties here
  },
  spacing: 10
});

export default theme;
